#Debian based:
sudo apt-mark showhold
dpkg --get-selections | grep hold
sudo apt-mark hold firefox
sudo apt-mark unhold firefox

apt-mark showauto [automatic installed packages]
apt-mark showmanual [manuel installed packages]

#Or:
vi /etc/apt/preferences
Package: firefox
Pin: release *
Pin-Priority: -1
