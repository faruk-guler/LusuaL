##############
## fstrim:
###############

fstrim, bir dosya sisteminde kullanılmayan (boş) veri bloklarını tespit ederek, bu blokları depolama cihazına bildiren bir komuttur.
Bu işlem, SSD'lerin ve diğer flash tabanlı depolama cihazlarının performansını ve ömrünü artırmak için kullanılır.
TRIM işlemi, cihazın hangi blokların artık kullanılmadığını bilmesini sağlayarak, bu blokları sıfırlamak veya yeniden yazma için hazır hale getirmek için kullanılır.

Kontrol etmek için: systemctl status fstrim.timer
Manuel olarak çalıştırmak için: sudo fstrim -av sudo fstrim -v /

Linux'ta fstrim komutuyla manuel trimleme yapılabilir veya fstrim.timer servisini etkinleştirerek otomatik hale getirilebilir.
sudo systemctl enable --now fstrim.timer

Hangi diskleri ve dosya sistemlerini trimlemek gerektiğini görmek için lsblk --discard komutunu çalıştırabilirsiniz.

sblk --discard

sudo systemctl enable fstrim.timer
sudo systemctl start fstrim.timer
